import { actions } from '~/action'
import { mutations } from '~/mutations'
import { getters } from '~/getters'

const state = {
  user: null,
}
export default {
  state,
  actions,
  mutations,
  getters,
  }
