import { ref } from "vue";
import * as auth from "firebase/auth";

const user = ref(auth.getAuth().currentUser);
auth.getAuth().onAuthStateChanged(function (_user) {
  if (_user) user.value = _user;
});

function getUser() {
  return { user };
}

export function useUser() {
  return { getUser };
}
